server:
  port: 9898

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/placement_db
    username: postgres
    password: Finytive@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

app:
  jwt:
    secret: UniqPlacementConsoleSecretKeyForJwtTokenGeneration2024VeryLongSecretKey
    expiration-ms: 86400000  # 24 hours
